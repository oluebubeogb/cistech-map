/**
 * search/landmarks.js — nearest landmarks (API surface).
 */
(function (global) {
  'use strict';
  var Mahp = global.Mahp || (global.Mahp = {});
  Mahp.landmarks = {
    setup: null
  };
})(typeof window !== 'undefined' ? window : globalThis);

/**
 * search/mark-area.js — mark administrative / named areas (API surface).
 */
(function (global) {
  'use strict';
  var Mahp = global.Mahp || (global.Mahp = {});
  Mahp.search = Mahp.search || {};
  Mahp.markArea = {
    setup: null
  };
})(typeof window !== 'undefined' ? window : globalThis);
